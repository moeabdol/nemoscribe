[project]
name = "nemoscribe"
version = "0.1.0"
description = "Multi-lingual CLI transcriber (batch + live streamin) built on NVIDIA Nemotros 3.5 ASR"
requires-python = ">=3.12"
dependencies = ["torch>=2.13", "transformers>=5.13"]
